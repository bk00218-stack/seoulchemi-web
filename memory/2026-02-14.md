# 2026-02-14 작업 기록

## 서울케미 웹앱 작업

### 다크모드 CSS 문제 발견 및 수정 시도
- **문제**: 윈도우 다크모드일 때 웹앱 텍스트가 흰색 배경에 흰색으로 안 보임
- **원인**: 컴포넌트들이 배경색을 하드코딩(`#ffffff`)하고, 텍스트는 CSS 변수(`var(--text-primary)`) 사용
  - 다크모드 시 `--text-primary`가 `#ffffff`(흰색)으로 바뀌지만 배경은 그대로 흰색
- **1차 수정**: 서브에이전트가 89개 파일 CSS 변수로 변환 (`f1fa265`)
- **문제 발생**: UTF-8 인코딩 손상으로 한글이 깨짐 (`?�` 등)
- **롤백**: `aa6985c` 커밋으로 전체 복구 (`372fe33`)

### 비관리자 시스템 정리
- **문제**: `/` (비관리자)와 `/admin/` (관리자) 두 개의 별도 시스템 존재
  - Zeus가 `/purchase/vendors`로 잘못 접근하여 혼란
- **1차 수정**: 47개 비관리자 파일 삭제 (`77a34f5`) - 빌드 실패
- **원인**: `notifications/page.tsx`가 삭제된 `Layout.tsx` import
- **최종 해결**: 
  - 파일 복구 후 로그인 리다이렉트만 `/admin`으로 변경
  - 루트 페이지 `/`를 `/admin`으로 리다이렉트하는 단순 페이지로 교체
  - 커밋: `43bb58f`

### 최종 상태
- **사이트 URL**: https://mobile-glass.vercel.app
- **작동 확인**: 로그인 → `/admin` 대시보드 정상 표시
- **미해결**: 다크모드 CSS 수정 (UTF-8 인코딩 문제로 롤백됨)
  - 다음에 조심해서 다시 적용 필요

### 기술적 교훈
- PowerShell에서 파일 수정 시 UTF-8 인코딩 주의 필요
- 대량 파일 수정 시 인코딩 손상 위험 있음
- 삭제 전 import 의존성 확인 필수

## Zeus 정보
- 내일(2/15) 쉬는 날
- 늦은 시간까지 작업 (23:57 KST)
