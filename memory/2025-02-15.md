# 2025-02-15 작업 로그

## 서울케미 웹앱 (mobile-glass) Vercel 배포 에러 해결 🎉

### 문제
Vercel 배포 시 TypeScript 빌드 에러 - sidebar import 누락

### 원인
`constants/sidebar.ts`에서 export하는 sidebar 상수들을 페이지에서 사용하면서 import를 안 함

### 해결한 파일들 (총 30개 이상)

**Products 폴더:**
- bundles, items, rx, shortcuts, stock/bulk, stock/optimal

**Purchase 폴더:**
- new, page, settlement, settlement/history, vendors, vendors/unpaid

**Settings 폴더:**
- accounts, categories, main, menu-permissions, product-detail, shipping

**Stats 폴더:**
- page, etc, groups, products, shipping

**Stores 폴더:**
- page, approve, delivery-staff, groups, groups/discounts, groups/types, notices, settle, verify, [id]

### 수정 방법
각 페이지에 해당 sidebar import 추가:
```tsx
import { PRODUCTS_SIDEBAR } from '../../constants/sidebar'
import { PURCHASE_SIDEBAR } from '../../constants/sidebar'
import { SETTINGS_SIDEBAR } from '../../constants/sidebar'
import { STATS_SIDEBAR } from '../../constants/sidebar'
import { STORES_SIDEBAR } from '../../constants/sidebar'
```

### 작성한 스크립트
- `fix-sidebar-imports.js` - purchase, settings 일괄 수정
- `fix-stores-imports.js` - stores 일괄 수정

### 최종 성공 커밋
- `7809a89` - fix: Add missing STORES_SIDEBAR imports to stores pages
- 배포 성공: TAgj4a5Lt (1분 15초)

### 교훈
리팩토링 시 모든 파일의 import 확인 필요. 특히 하위 폴더 페이지들은 depth에 따라 상대경로가 달라짐 (`../`, `../../`, `../../../`)
