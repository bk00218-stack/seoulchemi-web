# 2025-02-15 작업 노트

## 서울케미 웹앱 - 가맹점 관리 페이지 UI 수정

### 수정 요청 및 적용
1. **테이블 헤더 폰트 키우기** ✅
   - 14px, fontWeight: 600
   - `whiteSpace: 'nowrap'`으로 한 줄 유지

2. **검색 필터 → 헤더 밑에 박스로** ✅
   - 각 컬럼 아래에 검색 input 배치
   - 코드, 안경원명, 대표자, 연락처, 주소 검색 가능

3. **통계 카드 크기 줄이기** ✅
   - padding: 14px 16px
   - 숫자: 20px
   - 이모지: 22px

4. **주소 검색 추가** ✅
   - searchAddress state 추가
   - 검색 API에 포함

5. **버튼 가로 배치** 🔄 진행중
   - `flexDirection: 'row'`, `flexWrap: 'nowrap'` 적용
   - 관리 컬럼 너비 220px로 확대
   - 테이블 minWidth 1000px + overflow: auto (가로 스크롤)
   - 화면 너비에 따라 여전히 세로로 보일 수 있음

### 커밋 내역
- `ui: stores - header font bigger, search under columns, smaller stat cards`
- `fix: stores table - single line headers, address search, horizontal buttons`
- `fix: wider action column for horizontal buttons`
- `fix: table horizontal scroll with minWidth`

### 남은 이슈
- 버튼이 화면 너비에 따라 세로로 보이는 문제
- 해결 방안: 버튼 텍스트를 아이콘으로 변경하거나 관리 컬럼 더 넓히기
